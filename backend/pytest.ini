[pytest]
addopts = -v
pythonpath = .
asyncio_mode = auto
python_files = test_*.py
testpaths = tests
env_files = .env.test
markers =
    e2e: End-to-end tests against live server (may cost OpenAI tokens)
    smoke: Quick smoke tests (subset of e2e)